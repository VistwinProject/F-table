# F-table 桌面端 — 如何運行

AI 大腦控制塔的「桌面投影」前端 + NFC 讀卡機 server。

---

## 換新電腦：第一次設定（只做一次）

### 1. 先裝兩個東西

| 軟體 | 下載 | 注意 |
|---|---|---|
| **Node.js 18+** | https://nodejs.org | 直接下一步安裝即可 |
| **Python 3.11+** | https://python.org | ⚠️ 安裝時**務必勾選 `Add Python to PATH`** |

裝完**重開機**（或至少重開命令視窗），讓 PATH 生效。

### 2. 雙擊 `setup.bat`

它會自動：
- 檢查 Node / Python 有沒有裝好
- 安裝 Python 套件（pyscard、websockets）
- 安裝前端套件（npm install，第一次比較久，約 1–2 分鐘）

看到 `Setup complete` 就成功了。

---

## 每次要開：雙擊 `start.bat`

它會：
1. 開一個黑視窗跑 **NFC server**（port 8787）
2. 開一個黑視窗跑 **前端網頁**（port 5173）
3. 等 7 秒，自動用 **Chrome 全螢幕**打開 `http://localhost:5173`

畫面會停在**歡迎頁**，等平板按「開始」後才進互動頁。

> 開發測試時：也可以直接點桌面歡迎頁任意位置進入（正式投影時這個 click 不影響，因為投影面不能觸控）。

---

## 關閉：雙擊 `stop.bat`

會停掉 8787 + 5173 兩個 server。Chrome 視窗請自己按 **Alt+F4** 關。

（或者直接把那兩個黑色 server 視窗關掉也可以。）

---

## Chrome 操作

| 動作 | 按鍵 |
|---|---|
| 退出全螢幕 | `F11` |
| 關閉視窗 | `Alt + F4` |
| 重新整理 | `F5` 或 `Ctrl + R` |

---

## NFC 讀卡機

- 支援最多 **9 台 ACR122U**，USB 插上即可，server 會自動偵測、自動分配位置。
- **強烈建議用「有外接電源的 USB Hub」**，多台讀卡機同時供電才穩。
- 沒插讀卡機也能開（畫面正常顯示，只是不會有感應反應）。
- 新增卡片對應的家電：編輯 `server/uid-map.json`，加 `UID → { id, label, ... }`，存檔後重開 server。

---

## 三畫面同步（桌面 / 牆面 / 平板）

三個畫面都連同一個 NFC server（`ws://localhost:8787`）：

| 畫面 | 網址 |
|---|---|
| 桌面（這個 repo） | `http://localhost:5173` |
| 牆面 | `http://localhost:5174` |
| 平板 | `http://localhost:5175` |

平板按「開始」→ 桌面 + 牆面同步進入互動頁。詳細協議見專案的 `SYNC-SPEC.md`。

---

## 常見問題

**Q：start.bat 閃一下就關了 / 說找不到 Node 或 Python**
→ 沒裝好或沒加進 PATH。重裝、勾選 Add to PATH、重開機，再跑一次 `setup.bat`。

**Q：Chrome 沒自動全螢幕**
→ 如果 Chrome 本來就開著，新視窗的全螢幕參數會被忽略。先把 Chrome 全關，再跑 start.bat；或開了之後自己按 F11。

**Q：畫面打不開（網址錯誤頁）**
→ server 還沒起來。等幾秒重新整理（F5）。若持續，看那兩個黑視窗有沒有報錯。

**Q：左上角顯示「系統離線 / NFC service offline」**
→ 前端連不到 8787 server。確認 NFC server 那個黑視窗有在跑、沒報錯。
